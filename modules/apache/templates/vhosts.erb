<VirtualHost <%= hostname %>:<%= portnum %>>
  ServerName <%= ServerName %>
  <Location <%= Location %>
    ProxyPass <%= ProxyPass %>
    ProxyPassReverse <%= ProxyPass %>
  </Location>
</VirutalHost>